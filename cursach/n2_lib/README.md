# 📚 Система управления библиотекой (Python + PostgreSQL + SQLAlchemy + Streamlit)

## 📦 Описание

Многофункциональная клиент-серверная система для библиотеки, с поддержкой ролей:
- 👤 **Читатель**: может видеть свою карточку, текущие книги и зал.
- 🧑‍🏫 **Библиотекарь**: может управлять книгами, читателями, залами и выдачей книг.

Поддерживаются:
- регистрация/авторизация по логину и паролю;
- CRUD-операции со всеми объектами;
- посадка/высадка читателей из залов;
- выдача и возврат книг;
- отчёты: свободные места в зале, книги в 1 экз., топ-рейтинг книг и др.

---

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
pip install -r requirements.txt
````

### 2. Создайте базу данных PostgreSQL

```sql
CREATE DATABASE librarydb;
```

### 3. Настройте `database.ini`

```ini
[postgresql]
host=localhost
database=librarydb
user=postgres
password=ВАШ_ПАРОЛЬ
```

### 4. (Необязательно) Заполните тестовыми данными

```bash
python db.py
```

### 5. Запустите приложение

```bash
streamlit run main.py
```

---

## 🗂 Структура проекта

| Файл               | Описание                                    |
| ------------------ | ------------------------------------------- |
| `models.py`        | Модели таблиц SQLAlchemy                    |
| `procedures.py`    | Функции: регистрация, книги, залы, отчёты   |
| `main.py`          | Графический интерфейс с ролями на Streamlit |
| `db.py`            | (опционально) генерация тестовых данных     |
| `database.ini`     | Конфигурация подключения к PostgreSQL       |
| `requirements.txt` | Зависимости Python                          |

---

## 🔐 Роли пользователей

| Роль     | Доступ                                       |
| -------- | -------------------------------------------- |
| `reader` | Просмотр своих книг и зала                   |
| `admin`  | Полный доступ: книги, читатели, залы, отчёты |

---

## 📊 Реализованные отчёты

* Какие книги выданы каждому читателю
* Сколько свободных мест в каждом зале
* Можно ли выдать книгу (есть ли экземпляры)
* Кол-во книг заданного автора в зале
* Читатели, взявшие книги в одном экземпляре
* Книги с максимальным рейтингом

---

## 🧪 Пример логинов

| Роль         | Логин    | Пароль     |
| ------------ | -------- |------------|
| Библиотекарь | `admin`  | `admin123` |
| Читатель     | `reader1` | `1234`     |

(создаются при регистрации)

